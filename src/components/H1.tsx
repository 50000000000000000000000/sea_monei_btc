import { Typography } from "@mui/material";

const H1 = () => {
  return (
    <Typography textTransform="capitalize" component="h1" variant="h2" textAlign="center" fontWeight={600}>
      Diversify into the crypto market with a single token
    </Typography>
  );
};

export default H1;
